<UserControl x:Class="MyAsset.Wpf.Views.AssetToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/MyAsset.Wpf;component/Themes/MyAssetHandyTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Padding="12"
            Background="{StaticResource AssetPanelBrush}"
            BorderBrush="{StaticResource AssetBorderBrush}"
            BorderThickness="1"
            CornerRadius="6">
        <StackPanel>
            <TextBlock FontSize="14"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource AssetTextBrush}"
                       Text="资产工具" />
            <TextBox Margin="0,8,0,8"
                     Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}" />
            <Button Command="{Binding ShowInfoCommand}" Content="提示" />
        </StackPanel>
    </Border>
</UserControl>
