<Window x:Class="WaferSystem.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:views="clr-namespace:WaferSystem.Wpf.Views"
        Title="晶圆测试系统登录"
        Width="1280"
        Height="800"
        MinWidth="1180"
        MinHeight="720"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        FontFamily="Microsoft YaHei UI"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        TextOptions.TextHintingMode="Fixed"
        Background="{StaticResource PageBgBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="8"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Grid SnapsToDevicePixels="True">
        <views:LoginPage x:Name="LoginPage" LoginRequested="LoginPage_LoginRequested" />
        <Border Height="48"
                VerticalAlignment="Top"
                Background="Transparent"
                MouseLeftButtonDown="TitleDragArea_MouseLeftButtonDown" />
        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Orientation="Horizontal" Margin="0,8,8,0">
            <Button Click="MinimizeButton_Click" Style="{StaticResource LoginWindowButtonStyle}" ToolTip="最小化">
                <Path Data="M1,6 L11,6"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"
                      StrokeThickness="1.8"
                      Stretch="Uniform"
                      Width="12"
                      Height="12" />
            </Button>
            <Button Click="CloseButton_Click" Style="{StaticResource LoginWindowCloseButtonStyle}" ToolTip="关闭">
                <Path Data="M2,2 L10,10 M10,2 L2,10"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"
                      StrokeThickness="1.8"
                      Stretch="Uniform"
                      Width="12"
                      Height="12" />
            </Button>
        </StackPanel>
    </Grid>
</Window>
